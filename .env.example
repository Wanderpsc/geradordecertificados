# Configurações do Servidor
PORT=5000
NODE_ENV=production

# Banco de Dados MongoDB
MONGODB_URI=mongodb://localhost:27017/certificados_db
# Para MongoDB Atlas (produção):
# MONGODB_URI=mongodb+srv://usuario:senha@cluster.mongodb.net/certificados_db

# JWT Secret (MUDE ESTE VALOR PARA ALGO SEGURO)
JWT_SECRET=sua_chave_super_secreta_aqui_mude_isso_123456789

# Configurações de Licença
LICENSE_ENCRYPTION_KEY=chave_criptografia_licenca_mude_isso_987654321

# CORS - Domínios permitidos (separados por vírgula)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5000

# Configurações de Email (opcional - para envio de licenças)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=seu_email@gmail.com
SMTP_PASS=sua_senha_app

# ─── Mercado Pago ───────────────────────────────────────────
# Obtenha em: https://www.mercadopago.com.br/developers/panel/app
# Use credenciais de TESTE para sandbox / PRODUÇÃO para live
MP_ACCESS_TOKEN=APP_USR-0000000000000000-000000-00000000000000000000000000000000-000000000

# URL base da API (backend) — usado para webhook e redirect de cartão
API_BASE_URL=https://gerador-certificados.onrender.com

# URL base do frontend — usado para back_urls do checkout de cartão
APP_BASE_URL=https://gerador-certificados.surge.sh

# ─── Email (Nodemailer) ──────────────────────────────────────
# Gmail: ative "Senhas de app" em https://myaccount.google.com/apppasswords
# Outros provedores: Brevo (smtp-relay.brevo.com:587), Mailersend, etc.
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=seu@gmail.com
EMAIL_PASS=sua_senha_de_app_gmail
# Email do admin que recebe notificações de venda (padrão: EMAIL_USER)
EMAIL_ADMIN=wanderpsc@gmail.com
